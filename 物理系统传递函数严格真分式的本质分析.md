# 为什么常见物理系统的传递函数分子阶次低于分母？

## 1 问题的精确表述

一个线性时不变（LTI）连续系统的传递函数写作

$$G(s) = \frac{B(s)}{A(s)} = \frac{b_m s^m + b_{m-1}s^{m-1} + \cdots + b_0}{a_n s^n + a_{n-1}s^{n-1} + \cdots + a_0}$$

- 若 $m < n$，称为 **严格真分式（strictly proper）**
- 若 $m = n$，称为 **真分式（proper）**
- 若 $m > n$，称为 **非真分式（improper）**

**经验事实：** 绝大多数物理系统的传递函数满足 $m < n$，即为严格真分式。少数满足 $m = n$，而 $m > n$ 几乎不在真实物理系统中出现。

本文从**物理**、**数学**、**信号**三个层面，系统解释这一现象背后的深层原因。

---

## 2 物理层面的解释

### 2.1 因果律与有限传播速度

物理世界的一条基本法则是**因果律（causality）**：效果不能先于原因出现。当系统输入发生变化时，输出不可能瞬间以无穷快的速率跟随。

从传递函数的角度看，系统的**高频增益**为

$$\lim_{|s| \to \infty} G(s) = \lim_{|s| \to \infty} \frac{b_m s^m + \cdots}{a_n s^n + \cdots}$$

- 若 $m < n$：$\lim_{|s|\to\infty} G(s) = 0$ — 高频信号被衰减
- 若 $m = n$：$\lim_{|s|\to\infty} G(s) = b_m/a_n$ — 有限常数
- 若 $m > n$：$\lim_{|s|\to\infty} G(s) = \infty$ — 高频信号被**无限放大**

$m > n$ 意味着系统对频率越高的信号响应越大——即对输入的变化率做放大。这在物理上是不可能的：任何物理介质对信号的传播都有有限带宽，不可能无限放大高频分量。

### 2.2 能量存储元件产生惯性

物理系统中的**能量存储元件**是传递函数分母阶次的根本来源：

| 领域 | 储能元件 | 状态变量 | 贡献的分母阶次 |
|------|---------|---------|:------------:|
| 电气 | 电容 $C$ | 电压 $v_C$ | +1 |
| 电气 | 电感 $L$ | 电流 $i_L$ | +1 |
| 机械（平动） | 质量 $m$ | 速度 $v$ | +1 |
| 机械（转动） | 转动惯量 $J$ | 角速度 $\omega$ | +1 |
| 热学 | 热容 $C_{th}$ | 温度 $T$ | +1 |
| 流体 | 容器体积 | 液位 $h$ | +1 |

**每一个独立的储能元件引入一个状态变量，对应传递函数分母增加一阶。**

而分子阶次取决于输入到输出之间的"直达"路径和能量存储元件的耦合方式。物理系统中，输入信号通常需要先经过至少一个储能元件才能到达输出，因此分子阶次严格低于分母阶次。

### 2.3 具体示例：RLC 电路

考虑串联 RLC 电路，以输入电压 $V_{in}(s)$ 为输入，以电容电压 $V_C(s)$ 为输出：

$$G(s) = \frac{V_C(s)}{V_{in}(s)} = \frac{1/(LC)}{s^2 + (R/L)s + 1/(LC)}$$

- 分母阶次 $n = 2$：对应两个储能元件（$L$ 和 $C$）
- 分子阶次 $m = 0$：输入电压需要先流过电感、再给电容充电，才能在电容上建立输出电压，不存在直达路径

若改为以电感电流为输出：

$$G(s) = \frac{I_L(s)}{V_{in}(s)} = \frac{Cs}{LCs^2 + RCs + 1} = \frac{(1/L)s}{s^2 + (R/L)s + 1/(LC)}$$

分子阶次升至 $m = 1$，但仍然 $m < n = 2$。直觉上：电流是电容电压的微分（$i = C\frac{dv_C}{dt}$），微分操作让分子多了一个 $s$，但不会超过分母阶次。

### 2.4 具体示例：直流电机

直流电机从电枢电压 $V_a$ 到转速 $\omega$ 的传递函数：

$$G(s) = \frac{\Omega(s)}{V_a(s)} = \frac{K_t}{(Ls + R)(Js + B) + K_t K_e}$$

展开分母：

$$= \frac{K_t}{LJs^2 + (LB + RJ)s + (RB + K_t K_e)}$$

- 分母 $n = 2$：来自电感 $L$（电气储能）和转动惯量 $J$（机械储能）
- 分子 $m = 0$：电压必须先驱动电流（通过 $L$），电流再产生力矩，力矩再加速转子（通过 $J$），层层传递，无法跳过

### 2.5 具体示例：弹簧-质量-阻尼器

外力 $F$ 作用下质量块的位移 $x$：

$$G(s) = \frac{X(s)}{F(s)} = \frac{1}{ms^2 + cs + k}$$

- 分母 $n = 2$：质量 $m$ 存储动能，弹簧 $k$ 存储势能
- 分子 $m = 0$：力必须先加速质量块，质量块才能产生位移

---

## 3 数学层面的解释

### 3.1 状态空间表示的视角

任何有理传递函数都可以用状态空间模型实现：

$$\dot{\mathbf{x}} = A\mathbf{x} + B\mathbf{u}$$
$$\mathbf{y} = C\mathbf{x} + D\mathbf{u}$$

传递函数为

$$G(s) = C(sI - A)^{-1}B + D$$

由 Cramer 法则，$(sI - A)^{-1} = \frac{\text{adj}(sI - A)}{\det(sI - A)}$，其中：

- $\det(sI - A)$ 是 $n$ 阶多项式（$n$ 为状态维数）→ 贡献分母
- $\text{adj}(sI - A)$ 的每个元素最高为 $n-1$ 阶多项式

因此 $C(sI - A)^{-1}B$ 的分子阶次**最多为 $n-1$**，即严格真分式。

**直馈项 $D$ 的作用：** 只有当 $D \neq 0$ 时，分子阶次才可能等于分母阶次（$m = n$），使传递函数变为真分式但不再严格真分。$D$ 表示输入到输出的**直接穿透**，不经过任何储能/状态环节。

- 物理系统中 $D = 0$ 是常态（输入必须经过储能元件），所以 $m < n$
- $D \neq 0$ 的例子：纯电阻分压器（无储能元件，但这时传递函数是常数 $G(s) = k$，$m = n = 0$）

### 3.2 脉冲响应的视角

传递函数是脉冲响应 $g(t)$ 的 Laplace 变换：

$$G(s) = \mathcal{L}\{g(t)\}$$

考虑分子分母阶次对脉冲响应起始行为的影响：

- 若 $m < n$：$g(0^+) = 0$，脉冲响应从零开始平滑上升
- 若 $m = n$：$g(0^+) = b_n/a_n \neq 0$，脉冲响应初值有限
- 若 $m > n$：$g(t)$ 包含 $\delta(t)$ 及其导数，脉冲响应含有奇异分量

物理系统的输出不可能出现理想冲激函数——这需要在无穷短时间内传递有限能量，违反了物理可实现性。因此 $m > n$ 被排除。进一步，物理系统的输出通常不能瞬间跳变（因为储能元件的状态不能突变），所以 $g(0^+) = 0$，对应 $m < n$。

### 3.3 从微分方程的阶次看

设系统的输入-输出关系为

$$a_n y^{(n)} + a_{n-1}y^{(n-1)} + \cdots + a_0 y = b_m u^{(m)} + b_{m-1}u^{(m-1)} + \cdots + b_0 u$$

右端 $b_m u^{(m)}$ 表示系统需要对输入做 $m$ 阶微分。

在物理上，对一个信号做精确微分是不可能的：

1. **噪声放大**：微分运算放大高频噪声，$m$ 阶微分将噪声功率按 $\omega^{2m}$ 放大
2. **信息需求**：$m$ 阶微分需要预知信号未来的变化趋势（在数值实现中），这违反因果性
3. **能量约束**：高阶微分意味着系统对输入变化率的高阶响应，需要无穷大带宽，不可物理实现

因此物理系统的 $m$ 总是受限的，不可能超过（也很少等于）$n$。

---

## 4 频域与信号层面的解释

### 4.1 物理系统是低通的

几乎所有物理系统本质上都是**低通系统**或**带通系统**，其幅频特性在高频段衰减。

严格真分式的幅频特性：

$$|G(j\omega)| \sim \frac{1}{\omega^{n-m}} \quad (\omega \to \infty)$$

当 $m < n$ 时，$|G(j\omega)|$ 以 $-20(n-m)$ dB/decade 的速率衰减。这意味着系统的**相对阶次** $r = n - m$ 决定了高频滚降的速率：

- $r = 1$：$-20$ dB/dec
- $r = 2$：$-40$ dB/dec
- $r = 3$：$-60$ dB/dec

物理世界中的能量耗散和惯性总是导致高频衰减，因此 $r \geq 1$（即 $m < n$）是物理系统的普遍特征。

### 4.2 有限能量约束

对于一个因果稳定系统，若脉冲响应 $g(t)$ 属于 $L^2$（有限能量），则

$$\int_0^\infty |g(t)|^2\,dt < \infty$$

由 Parseval 定理：

$$\int_{-\infty}^{+\infty} |G(j\omega)|^2\,d\omega < \infty$$

这要求 $|G(j\omega)|^2$ 在 $\omega \to \infty$ 时衰减得足够快，具体地需要 $n - m \geq 1$。若 $m \geq n$，则 $|G(j\omega)|$ 不趋于零（或趋于无穷），上述积分发散，意味着脉冲响应能量无穷大——物理上不可能。

### 4.3 有限功率与带宽

更直观地说：任何物理系统可以传递的最大功率是有限的，这意味着系统不可能对任意高频信号都保持不衰减的传输。电子放大器有增益带宽积限制，机械系统有共振频率以上的自然衰减，热系统的热扩散天然是低通滤波器。这些都对应 $m < n$。

---

## 5 严格真分式条件不满足时的后果

### 5.1 若 $m = n$（真分式，非严格真分）

传递函数可分解为：

$$G(s) = \frac{b_n}{a_n} + G_{\text{sp}}(s)$$

其中 $G_{\text{sp}}(s)$ 是严格真分式。常数项 $b_n/a_n$ 表示**直馈通道**。

物理上的含义：输入的一部分可以不经过任何储能环节直接到达输出。这在某些系统中是允许的（如含有直接电阻耦合路径的电路），但不是主流情况。

此时脉冲响应 $g(t) = \frac{b_n}{a_n}\delta(t) + g_{\text{sp}}(t)$，包含一个冲激项。

### 5.2 若 $m > n$（非真分式）

$$G(s) = d_{m-n}s^{m-n} + \cdots + d_1 s + d_0 + G_{\text{sp}}(s)$$

脉冲响应包含 $\delta(t)$ 的导数：

$$g(t) = d_{m-n}\delta^{(m-n)}(t) + \cdots + d_1\delta'(t) + d_0\delta(t) + g_{\text{sp}}(t)$$

**物理后果：**

1. 需要在零时间内传递/产生无穷大幅值的信号
2. 系统需要对输入做纯微分，而纯微分器物理上不可实现
3. 实际中总存在寄生元件（寄生电容、寄生电感、微小质量等）将系统"正则化"为严格真分式

### 5.3 理想微分器为什么不存在

理想微分器的传递函数 $G(s) = s$，有 $m = 1, n = 0$。

物理上不可实现的根本原因：

1. **无限带宽**：增益 $|G(j\omega)| = \omega$ 随频率线性增长，需要无限带宽
2. **无限噪声放大**：热噪声在高频段被无限放大
3. **能量违背**：对白噪声输入的输出功率为 $\int_0^\infty \omega^2 S_0\,d\omega = \infty$

实际工程中使用的"微分器"总是带有截止频率的高通滤波器：

$$G(s) = \frac{s}{\tau s + 1}$$

这是严格真分式（$m = 1, n = 1$... 实际上 $m = n = 1$，真分式），高频增益有限为 $1/\tau$。

---

## 6 相对阶次的物理含义

定义**相对阶次**（relative degree）：

$$r = n - m$$

$r$ 在控制理论中有重要的物理意义：

### 6.1 $r$ 等于输入到输出经过的最少储能级数

在状态空间中，$r$ 等于从输入 $u$ 到输出 $y$ 的**最短因果路径**上经过的积分器（储能元件）个数。

形式化地，$r$ 是满足以下条件的最小正整数：

$$CA^{r-1}B \neq 0$$

即从输入经过 $r$ 步状态传递才第一次影响到输出。

### 6.2 示例

**双积分系统**（如力到位移）：

$$G(s) = \frac{1}{ms^2} \quad \Rightarrow \quad r = 2$$

力先产生加速度（第一次积分得速度），再积分得位移。最少经过 2 个积分器。

**一阶系统**（如 RC 低通）：

$$G(s) = \frac{1}{\tau s + 1} \quad \Rightarrow \quad r = 1$$

电压需通过电容的一次积分才建立输出。

---

## 7 与 z 域的联系

在将连续系统离散化时，严格真分式特性对离散化方法有重要影响。

### 7.1 ZOH 离散化保持严格真分式

若连续传递函数 $G(s)$ 是严格真分式（$m < n$），经零阶保持器离散化后的 $G(z)$ 也是严格真分式。

直观原因：ZOH 模拟实际的采样-保持过程，物理可实现性在离散域中保持。

### 7.2 双线性变换保持阶次关系

双线性变换 $s = \frac{2}{T}\frac{z-1}{z+1}$ 是 $s$ 到 $z$ 的一一映射：

- 连续系统每个 $s$ 因子变为 $\frac{z-1}{z+1}$ 的因子
- 分子分母的有理函数阶次关系保持
- 若 $G(s)$ 的相对阶次为 $r$，则 $G(z)$ 的相对阶次也为 $r$

### 7.3 对数字控制器设计的影响

在数字控制器设计中，被控对象的相对阶次 $r$ 决定了：

1. **最小可能的闭环延迟**：$r$ 个采样周期
2. **控制器结构约束**：无法用因果控制器完全消除 $r > 1$ 的系统零点
3. **内模控制（IMC）设计**：需要添加 $r$ 阶低通滤波器保证控制器可实现

---

## 8 总结

传递函数分子阶次低于分母阶次（$m < n$），**不是**数学上的巧合，而是以下物理规律的必然反映：

| 物理根源 | 对应的数学表现 |
|---------|-------------|
| 储能元件（$C, L, m, J, \ldots$）产生系统惯性 | 分母阶次 $n$ = 独立储能元件数 |
| 输入到输出必须经过储能环节 | 分子阶次 $m$ 受限于路径上的"捷径" |
| 因果律：效果不超前于原因 | 高频增益 $\|G(j\omega)\| \to 0$ |
| 有限带宽：物理介质不能传递无穷高频 | $n - m \geq 1$ 保证高频滚降 |
| 有限能量：脉冲响应能量有限 | Parseval 定理要求 $n > m$ |
| 纯微分器物理不可实现 | 不允许 $m > n$ |

**一句话总结：** 物理系统的惯性（储能）使分母积累阶次，而因果律和有限带宽限制了分子的阶次，两者共同导致了 $m < n$ 这一普遍特征。

---

## 参考文献

1. Åström K J, Murray R M. *Feedback Systems: An Introduction for Scientists and Engineers*. 2nd ed. Princeton University Press, 2021.
2. Khalil H K. *Nonlinear Systems*. 3rd ed. Prentice Hall, 2002. (Chapter 13: Feedback Linearization, relative degree)
3. Oppenheim A V, Willsky A S. *Signals and Systems*. 2nd ed. Prentice Hall, 1997.
4. Franklin G F, Powell J D, Emami-Naeini A. *Feedback Control of Dynamic Systems*. 8th ed. Pearson, 2019.
5. 胡寿松. *自动控制原理*. 第7版. 科学出版社, 2019.
